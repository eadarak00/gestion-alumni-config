server:
  port: 8081  

spring:
  datasource:
    url: jdbc:mysql://ms-user-db:3306/db_user?createDatabaseIfNotExist=true
    username: user
    password: passer123
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect

  application:
    name: ms-user

eureka:
  client:
    service-url:
      defaultZone: http://ms-discover:8761/eureka

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: DEBUG
